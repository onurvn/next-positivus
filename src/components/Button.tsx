const Button = () => {
  return (
    <button className="text-custom-dark border-custom-dark hover:bg-custom-dark flex cursor-pointer items-center justify-center rounded-lg border bg-white px-4 py-3 transition-all duration-300 ease-in-out hover:text-white">
      Request a quote
    </button>
  );
};

export default Button;
